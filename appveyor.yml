version: 1.0.{build}

os: Visual Studio 2019

platform: Any CPU

configuration:
  - Debug
  - Release

branches:
  only:
    - "app"

init:
  - git config --global core.autocrlf input

clone_folder: c:\projects\IFME

shallow_clone: true 

install:
  - cd c:\projects
  - git clone https://github.com/JamesNK/Newtonsoft.Json

build:
  parallel: true                  # enable MSBuild parallel builds
  project: IFME.sln               # path to Visual Studio solution or project
  verbosity: detailed             # MSBuild verbosity level : quiet|minimal|normal|detailed
